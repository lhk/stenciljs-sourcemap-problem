{
  "name": "sourcemap-problem",
  "private": true,
  "version": "0.0.1",
  "description": "sourcemap-problem",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "dependencies": {
    "@ionic/cli": "^6.0.2",
    "@ionic/core": "^4.11.7"
  },
  "devDependencies": {
    "@stencil/core": "^1.8.4"
  }
}
